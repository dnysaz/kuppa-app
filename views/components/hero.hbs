<section>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-16 mb-20">
        <div class="space-y-6">
            <h3 class="text-[10px] font-bold text-black uppercase tracking-widest border-b border-black pb-2 inline-block">Environment</h3>
            <ul class="space-y-4 text-[13px] text-neutral-600">
                <li class="flex justify-between items-center border-b border-neutral-100 pb-3">
                    <span class="text-neutral-400">Status</span>
                    <span class="font-medium text-black">Ready</span>
                </li>
                <li class="flex justify-between items-center border-b border-neutral-100 pb-3">
                    <span class="text-neutral-400">Database</span>
                    <span class="font-medium text-black">{{#if supabaseActive}}Connected{{else}}Disconnected{{/if}}</span>
                </li>
                <li class="flex justify-between items-center border-b border-neutral-100 pb-3">
                    <span class="text-neutral-400">Engine</span>
                    <span class="font-medium text-black">Node.js 23.7</span>
                </li>
            </ul>
        </div>

        <div class="space-y-6">
            <h3 class="text-[10px] font-bold text-black uppercase tracking-widest border-b border-black pb-2 inline-block">Resources</h3>
            <div class="space-y-4">
                {{#if supabaseActive}}
                <a href="{{route 'dashboard'}}" class="flex items-center justify-between text-[13px] hover:text-neutral-500 transition-colors border-b border-neutral-100 pb-3">
                    <span class="font-medium">Dashboard</span>
                    <span>&rarr;</span>
                </a>
                {{else}}
                <div class="flex items-center justify-between text-[13px] text-neutral-300 border-b border-neutral-100 pb-3 cursor-not-allowed">
                    <span class="font-medium">Dashboard</span>
                    <span>&times;</span>
                </div>
                {{/if}}
                <a href="https://github.com/ketutdana/kuppa-docs" target="_blank" class="flex items-center justify-between text-[13px] hover:text-neutral-500 transition-colors border-b border-neutral-100 pb-3">
                    <span class="font-medium">Documentation</span>
                    <span>&rarr;</span>
                </a>
            </div>
        </div>
    </div>

    {{#unless supabaseActive}}
    <div class="mt-16 space-y-12">
        <h3 class="text-[10px] font-bold text-black uppercase tracking-widest border-b border-black pb-2 inline-block">Setup Steps</h3>
        
        <div class="space-y-12">
            {{!-- Step 1 --}}
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                <div class="text-[10px] font-bold text-neutral-400 uppercase tracking-widest pt-1">01. Config</div>
                <div class="md:col-span-3 space-y-2">
                    <p class="text-[13px] leading-relaxed">Update your <code class="font-mono text-black">.env</code> file with your project credentials:</p>
                    <div class="bg-neutral-100 p-4 rounded text-[12px] font-mono text-neutral-600">
                        SUPABASE_URL = [Your Project URL]<br>
                        SUPABASE_KEY = [Your Project ANON_KEY]
                    </div>
                </div>
            </div>

            {{!-- Step 2 --}}
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                <div class="text-[10px] font-bold text-neutral-400 uppercase tracking-widest pt-1">02. Initialize</div>
                <div class="md:col-span-3 space-y-2">
                    <p class="text-[13px] leading-relaxed">Run the initialization command. The terminal will provide the SQL setup if needed:</p>
                    <div class="bg-black text-white p-4 rounded text-[12px] font-mono">
                        $ node kuppa db
                    </div>
                </div>
            </div>

            {{!-- Step 3 --}}
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                <div class="text-[10px] font-bold text-neutral-400 uppercase tracking-widest pt-1">03. Migrate</div>
                <div class="md:col-span-3 space-y-2">
                    <p class="text-[13px] leading-relaxed">Sync your local schema with Supabase:</p>
                    <div class="bg-black text-white p-4 rounded text-[12px] font-mono">
                        $ node kuppa migrate
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{/unless}}
</section>