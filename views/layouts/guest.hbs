<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{!-- SEO & Dynamic Title --}}
    <title>{{#if (get "title")}}{{get "title"}} | {{/if}}{{ appName }}</title>
    <meta name="description" content="{{get "description" default="Build something amazing with Kuppa.Js - The minimalist javascript supabase framework"}}">
    <link rel="icon" type="image/png" href="{{asset 'logo/1.3.png'}}">
    <link rel="apple-touch-icon" href="{{asset 'logo/1.3.png'}}">
    {{!-- Google Fonts: Inter --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    {{!-- Framework Styles --}}
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'ui-sans-serif', 'system-ui'],
                    },
                },
            },
        }
    </script>
    <style>
        [x-cloak] { display: none !important; }
        
        /* Selection color to match monochrome style */
        ::selection {
            background-color: #000;
            color: #fff;
        }

        /* Smooth scroll for purists */
        html { scroll-behavior: smooth; }
    </style>
</head>
<body class="bg-white text-black min-h-screen flex flex-col font-sans antialiased">

    <main class="flex-grow flex flex-col justify-center">
        {{{ body }}}
    </main>

</body>
</html>