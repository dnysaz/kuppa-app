<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    {{!-- SEO & Dynamic Title --}}
    <title>{{#if (get "title")}}{{get "title"}} | {{/if}}{{ appName }}</title>
    <meta name="description" content="{{get "description" default="Build something amazing with Kuppa.Js - The minimalist javascript supabase framework"}}">
    <link rel="icon" type="image/png" href="{{asset 'logo/1.3.png'}}">
    <link rel="apple-touch-icon" href="{{asset 'logo/1.3.png'}}">
    {{!-- Fonts: Menggunakan Inter agar sekelas dengan website Vercel/React --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    {{!-- Framework Styles --}}
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'ui-sans-serif', 'system-ui'],
                    },
                },
            },
        }
    </script>

    <style>
        [x-cloak] { display: none !important; }

        /* Custom selection color */
        ::selection {
            background-color: rgba(0, 0, 0, 0.05);
            color: #000;
        }

        /* Gaya Scrollbar Minimalist */
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { 
            background: #e5e5e5; 
            border-radius: 10px;
            border: 2px solid transparent;
            background-clip: content-box;
        }
        ::-webkit-scrollbar-thumb:hover { background: #d4d4d4; background-clip: content-box; }

        /* Smooth page transitions */
        .page-fade-in {
            animation: fadeIn 0.5s ease-out forwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(5px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="bg-white text-neutral-900 min-h-screen flex flex-col font-sans antialiased selection:bg-black selection:text-white">
    
    {{!-- Navbar Partial --}}
    {{> navbar }}

    {{!-- Main Content dengan animasi fade-in sederhana --}}
    <main class="flex-grow page-fade-in">
        {{{ body }}}
    </main>

    {{!-- Footer Partial --}}
    {{> footer }}

    {{!-- Alpine.js untuk fitur interaktif masa depan --}}
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</body>
</html>